[project]
name = "lazier-matmul"
version = "0.1.0"
description = "Add your description here"
readme = "README.md"
requires-python = ">=3.13"
dependencies = [
    "numpy>=2.3.4",
    "pytest>=9.0.1",
]

[dependency-groups]
dev = [
    "ruff>=0.14.5",
    "ty>=0.0.1a26",
]
